---
engines:
  bundler-audit:
    enabled: true
  duplication:
    enabled: true
    config:
      languages:
        - ruby
  fixme:
    enabled: true
  rubocop:
    enabled: true
    channel: rubocop-0-50
ratings:
  paths:
    - "**.erb"
    - "**.rb"
  exclude_paths:
    - "config/**/*"
